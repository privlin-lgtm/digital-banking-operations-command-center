// Retired: the "Case" (fraud/AML investigation) model was removed when the
// schema was rebuilt around the SRE/production-engineering domain (Service,
// Incident, Runbook, RCA, SLA...). This file is intentionally unmounted —
// delete this directory (and customers/, transactions/) once you've
// confirmed nothing else references it.
export {};
